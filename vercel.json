{
  "buildCommand": "npm install && cd client && npm install && vite build --outDir ../public --emptyOutDir",
  "outputDirectory": "public",
  "cleanUrls": false,
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ],
  "version": 2
}